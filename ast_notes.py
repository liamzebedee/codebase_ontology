# The AST has _fields
# This will differ for each type of AST object.
# get_children will return a list of children for an AST.Node

# If the attributes can have zero-or-more values (marked with an asterisk), the values are represented as Python lists. 